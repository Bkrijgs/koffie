# Build artifacts
/espresso
/espresso-arm
*.png.tmp
dashboard.png
espresso.log
espresso.run.log

# Vendored prebuilt fbink (drop one here to install offline)
/vendor/

# OS cruft
.DS_Store

# The embedded CA bundle MUST be tracked (root .gitignore excludes *.pem).
!internal/supa/cacert.pem
