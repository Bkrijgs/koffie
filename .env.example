# Supabase — vul deze in via Project Settings → API in je Supabase dashboard.
# Kopieer dit bestand naar `.env.local` voor lokale dev en zet ze in Vercel
# voor productie.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
